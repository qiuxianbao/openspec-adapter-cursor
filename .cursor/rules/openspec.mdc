---
description: 减少常见 LLM 编码错误的行为指南。在编写、审查或重构代码时使用，以避免过度复杂化、进行精准修改、呈现假设并定义可验证的成功标准。
globs: openspec/**/*
alwaysApply: false
---
# Karpathy 行为指南

减少常见 LLM 编码错误的核心准则。倾向于**谨慎、克制、精准**。

## 1. 编码前思考 (Think Before Coding)
**核心：不要假设。不要隐藏困惑。呈现权衡。**
- **明确假设**：实现前明确说明你的假设。如果不确定，**必须**询问用户。
- **呈现选项**：如果存在多种实现路径，列出 Trade-off（优劣势对比），不要默默替用户做决定。
- **提出异议**：如果存在更简单的原生方法，直接指出。
- **及时止损**：遇到需求模糊或矛盾处，立即停止并要求澄清。

## 2. 简洁优先 (Simplicity First)
**核心：用最少的代码解决问题。严禁过度设计。**
- **拒绝镀金**：不添加需求之外的功能，不为一次性代码创建抽象。
- **拒绝过度防御**：不为极小概率或不可能发生的场景编写冗长的错误处理。
- **优先复用**：优先使用标准库及项目中已有的工具类，绝不重复造轮子。
- **自我审查**：“资深架构师会觉得这过于复杂吗？”如果是，立即重写简化。

## 3. 精准修改 (Precise Modifications)
**核心：只碰必须碰的。控制 Diff 噪音。**
- **严禁格式化**：绝不“顺手改进”相邻的代码、注释或缩进格式。
- **不修没坏的东西**：不要重构与当前任务无关的正常代码。
- **风格一致**：严格匹配现有代码风格（命名、缩进、范式），即使你认为有更好的写法。
- **清理自己**：只删除因你的改动而产生的孤儿代码（无用导入/变量/函数）。发现原有的死代码，仅口头提醒，**不主动删除**。

## 4. 目标驱动执行 (Goal-Driven Execution)
**核心：定义成功标准。闭环验证。**
- **可验证目标**：将任务转化为可测试的步骤（如：“修复 bug” → “编写重现测试 → 修复代码 → 确保测试通过”）。
- **步骤计划**：对于复杂任务，先输出简短的执行计划：
  `1. [步骤] → 验证: [检查标准]`
- **独立循环**：依靠强有力的成功标准进行自我验证，而不是写完代码就扔给用户测试。


## 核心法则

## Spec 驱动（Code is Cheap, Context is Expensive）

代码是廉价的消耗品，文档（Spec）才是昂贵的核心资产。

1. **No Spec, No Code** — 没有 spec，不准写代码
2. **Spec is Truth** — spec 和代码冲突时，错的一定是代码
3. **Reverse Sync** — 执行中发现 spec 与实际不符，先修 spec 再修代码
4. **代码现状必须有出处** — 每个结论必须标注文件路径和类名/方法名，不接受“我认为”“通常来说”
5. **变更即记录** — 任何代码变更完成后都必须同步更新对应的 changes/ 文档

## Help提示
- 遇到环境问题或需要人为决策（如敏感操作、架构重大变更），立即暂停并向用户请求指示。


---
**检验标准**：每一行修改都必须能直接追溯到用户的原始请求；Diff 中没有任何无关的改动。