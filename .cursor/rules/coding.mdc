---
description: 具体编码规范
globs: openspec/**/*
alwaysApply: false
---

# 编码规范 (Coding Style)

> 本规范基于《阿里巴巴Java开发手册》编程规约进行整理与扩展，旨在统一团队代码风格，提升代码可读性与可维护性。

## 1. 命名风格 (Naming Conventions)

- **类名**：使用 UpperCamelCase 风格（如 `UserDO`，`XmlService`），以下情形例外：DO / BO / DTO / VO / AO / PO / UID 等。
- **方法名、参数名、成员变量、局部变量**：使用 lowerCamelCase 风格（如 `localValue`，`getHttpMessage()`），方法名通常以动词开头。
- **常量命名**：全部大写，单词间用下划线隔开，力求语义表达完整清楚（如 `MAX_STOCK_COUNT`）。
- **抽象类**：以 `Abstract` 或 `Base` 开头。
- **异常类**：使用 `Exception` 结尾。
- **测试类**：以它要测试的类名开始，以 `Test` 结尾。
- **包名**：统一使用小写，点分隔符之间有且仅有一个自然语义的英语单词。包名统一使用单数形式。
- **禁止**：严禁使用拼音与英文混合的方式，更不允许直接使用中文的方式。

## 2. 常量定义 (Constant Definition)

- **禁止魔法值**：不允许任何魔法值（即未经预先定义的常量）直接出现在代码中。
- **常量分类维护**：按功能进行归类，分开维护（如缓存相关常量放在 `CacheConsts` 下，系统配置相关放在 `ConfigConsts` 下）。

## 3. 代码格式 (Code Format)

- **大括号**：如果大括号内为空，则简洁地写成 `{}` 即可；如果是非空代码块，左大括号前不换行，左大括号后换行，右大括号前换行，右大括号后还有 `else` 等代码则不换行；表示终止的右大括号后必须换行。
- **空格**：左小括号和字符之间不出现空格；右小括号和字符之间也不出现空格。`if/for/while/switch/do` 等保留字与括号之间都必须加空格。任何二目、三目运算符的左右两边都需要加一个空格。
- **缩进**：采用 4 个空格缩进，禁止使用 tab 字符。
- **换行**：单行字符数限制不超过 120 个，超出需要换行，换行时遵循：
  - 第二行相对第一行缩进 4 个空格，从第三行开始，不再继续缩进。
  - 运算符与下文一起换行。
  - 方法调用的点符号与下文一起换行。
  - 方法调用中的多个参数需要换行时，在逗号后进行。

## 4. OOP 规约 (OOP Rules)

- **静态调用**：避免通过一个类的对象引用访问此类的静态变量或静态方法，直接用类名来访问即可。
- **覆写方法**：所有的覆写方法，必须加 `@Override` 注解。
- **包装类**：所有的 POJO 类属性必须使用包装数据类型；RPC 方法的返回值和参数必须使用包装数据类型；局部变量推荐使用基本数据类型。
- **对象比较**：所有整型包装类对象之间值的比较，全部使用 `equals` 方法比较（注意 `-128` 到 `127` 的缓存池问题）。
- **BigDecimal**：为了防止精度损失，禁止使用构造方法 `BigDecimal(double)` 的方式把 double 值转化为 `BigDecimal` 对象，推荐使用 `BigDecimal.valueOf(double)` 或者 `new BigDecimal(String)`。

## 5. 集合处理 (Collection Processing)

- **hashCode 和 equals**：只要重写 `equals`，就必须重写 `hashCode`。
- **集合转数组**：使用集合转数组的方法，必须使用集合的 `toArray(T[] array)`，传入的是类型完全一致、长度为 0 的空数组。
- **数组转集合**：使用工具类 `Arrays.asList()` 把数组转换成集合时，不能使用其修改集合相关的方法（`add`/`remove`/`clear`），会抛出 `UnsupportedOperationException`。
- **遍历删除**：不要在 `foreach` 循环里进行元素的 `remove`/`add` 操作。`remove` 元素请使用 `Iterator` 方式，如果并发操作，需要对 `Iterator` 对象加锁。
- **泛型**：集合初始化时，指定集合初始值大小（如 `new HashMap<>(16)`）。

## 6. 并发处理 (Concurrency)

- **单例模式**：获取单例对象需要保证线程安全，其中的方法也要保证线程安全。
- **线程池**：线程资源必须通过线程池提供，不允许在应用中自行显式创建线程。线程池不允许使用 `Executors` 去创建，而是通过 `ThreadPoolExecutor` 的方式，以明确线程池的运行规则，规避资源耗尽的风险。
- **日期格式化**：`SimpleDateFormat` 是线程不安全的类，一般不要定义为 `static` 变量，如果定义为 `static`，必须加锁，或者使用 `DateUtils` 工具类，推荐使用 JDK8 的 `DateTimeFormatter`。
- **ThreadLocal**：必须回收自定义的 `ThreadLocal` 变量，尤其在线程池场景下，线程经常会被复用，如果不清理自定义的 `ThreadLocal` 变量，可能会影响后续业务逻辑和造成内存泄露等问题。

## 7. 控制语句 (Control Statements)

- **switch**：在一个 `switch` 块内，每个 `case` 要么通过 `continue`/`break`/`return` 等来终止，要么注释说明程序将继续执行到哪一个 `case` 为止；在一个 `switch` 块内，都必须包含一个 `default` 语句并且放在最后。
- **if/else**：在 `if`/`else`/`for`/`while`/`do` 语句中必须使用大括号。即使只有一行代码，避免采用单行的编码方式。
- **嵌套深度**：表达异常的分支时，少用 `if-else` 方式，推荐卫语句。超过 3 层的 `if-else` 的逻辑判断代码可以使用卫语句、策略模式、状态模式等来实现。

## 8. 注释规约 (Comments)

- **类、属性、方法注释**：必须使用 Javadoc 规范，使用 `/**内容*/` 格式，不得使用 `// xxx` 方式。
- **方法内部注释**：在被注释语句上方另起一行，使用 `//` 注释。方法内部多行注释使用 `/* */` 注释，注意与代码对齐。
- **枚举**：所有的枚举类型字段必须要有注释，说明每个数据项的用途。

## 9. 异常处理 (Exception Handling)

- **异常捕获**：异常不要用来做流程控制，条件控制。
- **捕获大类**：捕获异常是为了处理它，不要捕获了却什么都不处理而抛弃之，如果不想处理它，请将该异常抛给它的调用者。最外层的业务使用者，必须处理异常，将其转化为用户可以理解的内容。
- **事务回滚**：有 `try` 块放到了事务代码中，`catch` 异常后，如果需要回滚事务，一定要注意手动回滚事务。
- **finally 块**：不能在 `finally` 块中使用 `return`。
- **自定义异常**：业务异常使用自定义异常（如 `MicroException`），携带错误码。系统异常向上抛出，由统一异常处理器兜底。
- **禁止吞掉异常**：catch 中必须记录日志。

## 10. 日志规约 (Logging)

- **日志框架**：应用中不可直接使用日志系统（Log4j、Logback）中的 API，而应依赖使用日志框架（SLF4J、JCL）中的 API。
- **日志级别与内容**：
  - Controller 入口打 INFO，含请求关键参数。
  - 异常打 ERROR，含完整堆栈。
- **性能优化**：输出的 POJO 类必须重写 `toString` 方法。在日志输出时，字符串变量之间的拼接使用占位符的方式。
- **敏感信息**：禁止在日志中打印用户敏感信息（如密码、身份证号等）。

## 11. 其他 (Others)

- **幂等性**：写接口必须考虑幂等。
- **同步策略**：涉及并发场景必须说明同步策略。
- **单一职责**：一个模块、类或函数只负责一件事情。
- **高内聚低耦合**：模块间的依赖应当尽量降到最低。
