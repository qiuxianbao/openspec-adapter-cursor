---
description: 安全红线，禁止做
globs: openspec/**/*
alwaysApply: false
---

# 安全红线 (Security Guidelines)

**⚠️ 注意：以下红线要求为必须项，禁止删除或绕过，任何调整必须经过人工统一确认！**

## 1. 凭据安全
- ❌ **绝对禁止**将密码、密钥、Token、AK/SK 等敏感信息硬编码在代码中。
- ❌ 所有凭据必须通过环境变量或配置中心注入。

## 2. 数据安全
- ❌日志中**严禁打印**敏感数据（如用户密码、身份证号、银行卡号等），必须进行脱敏处理。
- ❌防御 SQL 注入：所有数据库交互必须使用参数化查询或标准 ORM，严禁直接拼接 SQL 字符串。

## 3. 业务安全
- ⚠️ 敏感业务接口必须进行鉴权验证。
- ⚠️ 涉及资金变更的逻辑，必须在 spec 中明确标注，人工审查后方可编码
- ⚠️ 涉及状态流转的逻辑，必须检查状态机合法性
- ⚠️ 涉及权限变更的逻辑，必须显式校验操作人权限