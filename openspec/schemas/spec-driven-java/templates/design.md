## 1. 技术方案概述 (How)
- 简述如何实现 Spec 中定义的规格。
- 关键技术选型或架构调整说明。

## 2. 存储与数据流设计
- **数据库/表结构变更**：
- **缓存策略**：

## 3. 面向失败设计 (Design for Failure)
- **降级策略 (Fallback)**：[核心依赖故障时的降级方案]
- **熔断机制 (Circuit Breaker)**：[哪些接口需要熔断，阈值是多少]
- **幂等性设计 (Idempotency)**：[重试场景下的幂等键设计]
- **重试与死信队列 (Retry & DLQ)**：[异步/重试机制设计]

## 4. 接口定义 (API Design)
定义相关的对内/对外接口（建议遵循 RFC 9457 Problem Details 标准设计错误响应）：
- **接口路径**: `[POST] /api/v1/example`
- **请求参数**: 
  ```json
  {
    "id": "string"
  }
  ```
- **响应参数**:
  ```json
  {
    "code": 0,
    "data": {}
  }
  ```

## 5. 技术方案对比 (ADR)

### 方案 A：[名称]
- 技术栈：[具体技术]
- 优势：[1-2 条]
- 劣势：[1-2 条]
- 适合场景：[什么情况下选这个]
- 预估工作量：[大致范围]

### 方案 B：[名称]
...

### 建议与架构决策 (Architecture Decision Record)
- **推荐方案**：[说明推荐哪个方案]
- **Rationale (决策原因)**：[为什么选这个方案，解决了什么核心问题]
- **Trade-offs (妥协与权衡)**：[牺牲了什么，例如牺牲了强一致性换取可用性]

# 边界
- 必须提供 2-3 个方案并分析利弊
- 禁止在用户选定前开始实现
- 架构复杂度必须匹配项目实际规模