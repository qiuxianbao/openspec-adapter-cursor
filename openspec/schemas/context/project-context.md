# 项目上下文 (Project Context)

> **用途**：描述工程侧事实——技术栈、目录结构、模块依赖、分层架构。供 OpenSpec 全链路（propose → specs → design → tasks → apply）作为 AI 约束，**不得**写入 artifact 正文。
>
> **维护方式**：首次或结构变更后执行 `/opsx-init-ctx` 自动扫描填充；手动补充后再次执行将增量合并（不覆盖已有内容）。
>
> **与 domain.md 的分工**：本文件只写「怎么建、怎么跑」；业务规则、术语、状态机见 `domain.md`。

---

## 1. 项目概述

| 字段 | 内容 |
| --- | --- |
| 项目名 | （待填充） |
| 简介 | （一句话：行业 + 核心功能） |
| 仓库/模块根 | （如 monorepo 根目录或主模块路径） |

---

## 2. 技术栈

> **AI 填充指引**：扫描 `pom.xml` / `build.gradle` / `package.json` / `go.mod` 等；从 `application*.yml`、`bootstrap*.yml` 推断数据库、缓存、MQ、注册中心等外部依赖。

| 分类 | 组件 | 版本 | 作用 | 备注 |
| --- | --- | --- | --- | --- |
| 语言/运行时 | （如 Java 17） | | | |
| 构建工具 | （如 Maven / Gradle） | | | |
| 核心框架 | （如 Spring Boot） | | | |
| 数据库 | | | | |
| 缓存 | | | | |
| 消息队列 | | | | |
| 注册/配置中心 | | | | |
| 其他中间件 | | | | |

---

## 3. 目录结构

> **AI 填充指引**：扫描项目根目录，以 **tree 格式**展示核心模块（深度 2–4 层即可）；每个一级目录后用 `<!-- 职责 -->` 注释说明。复杂项目只保留业务相关目录，跳过 `node_modules`、`.git`、`target`、`build` 等。

```
（待填充，示例格式）
project-root/
├── module-a/          <!-- 职责说明 -->
│   ├── api/
│   └── service/
└── module-b/          <!-- 职责说明 -->
```

---

## 4. 模块依赖与上下游

> **AI 填充指引**：从构建文件（module 声明、依赖坐标）推断模块间调用关系；标注当前服务在链路中的位置（入口 / 编排 / 下游 / 基础设施）。

| 模块 | 职责 | 上游（依赖谁） | 下游（谁依赖它） | 备注 |
| --- | --- | --- | --- | --- |
| （待填充） | | | | |

---

## 5. 分层架构

> **AI 填充指引**：根据包结构或目录命名推断分层（如 Controller / Service / Repository / Domain）；说明各层职责与禁止的跨层调用。

### 5.1 分层说明

| 层级 | 包/目录 | 职责 | 约束 |
| --- | --- | --- | --- |
| （待填充） | | | |

### 5.2 技术架构示意

```s
（可选：ASCII 架构图，展示服务间调用与中间件）
```

---

## 6. 开发约定（工程侧）

> 与 `.cursor/rules/` 互补：此处只记录**本项目特有**的约定；通用编码规范不在此重复。

- （待填充，如：包命名规范、异常体系、日志 traceId 传递方式等）

---

## 7. 待确认 (TODO)

> init-ctx 无法从代码推断时保留 TODO，由人工补充。

- [ ] （待填充）
